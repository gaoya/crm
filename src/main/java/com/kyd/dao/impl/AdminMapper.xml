<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kyd.dao.AdminMapper">
    <update id="update" parameterType="java.util.Map">
        UPDATE sys_admin
        <include refid="UPDATE_SQL"/>
        WHERE id=#{id}
    </update>

    <insert id="insert" parameterType="java.util.Map">
        INSERT  INTO
        sys_admin
        (
            name,
            age,
            phone,
            address,
            email,
            qq,
            create_time,
            create_user,
            remark,
            use_flag,
            del_flag

        )
        VALUES
        (
            #{name},
            #{age},
            #{phone},
            #{address},
            #{email},
            #{qq},
            timestamp(now()),
            #{createUser},
            #{remark},
            #{useFlag},
            #{delFlag}

        )
    </insert>
    <delete id="deleteById" parameterType="java.lang.Long">
      DELETE  FROM sys_admin
      WHERE ID=#{id}
    </delete>

    <select id="findById" parameterType="java.lang.Long" resultType="java.util.Map">
      SELECT * FROM sys_admin
      WHERE ID=#{id}
    </select>

    <select id="findAllByParam" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
        <include refid="COLUMN_SQL"/>
        FROM sys_admin
        WHERE 1 =1
        <include refid="WHERE_SQL"/>
        limit
        <choose>
            <when test="startIndex!=null  and startIndex !=''">
                #{startIndex}
            </when>
            <otherwise>
               0
            </otherwise>
        </choose>
        ,
        <choose>
            <when test="endIndex!=null and endIndex !=''">
                #{endIndex}
            </when>
            <otherwise>
                10
            </otherwise>
        </choose>
    </select>

    <select id="findCount" parameterType="java.util.Map" resultType="java.lang.Long">
        select COUNT(1) from sys_admin
        WHERE 1=1
        <include refid="WHERE_SQL"/>
    </select>

    <select id="findAllByPageParam" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
        <include refid="COLUMN_SQL"/>
        FROM sys_admin
        WHERE 1=1
        <include refid="WHERE_SQL"/>
        limit
        <choose>
            <when test="startIndex!=null  and startIndex !=''">
                #{startIndex}
            </when>
            <otherwise>
                0
            </otherwise>
        </choose>
        ,
        <choose>
            <when test="endIndex!=null and endIndex !=''">
                #{endIndex}
            </when>
            <otherwise>
                10
            </otherwise>
        </choose>
    </select>

    <select id="findCountByPageParam" parameterType="java.util.Map" resultType="java.lang.Long">
        SELECT COUNT(1) size
        FROM sys_admin
        WHERE 1=1
        <include refid="WHERE_SQL"/>
    </select>

    <sql id="COLUMN_SQL">
        id as id,
        name as name,
        age as age,
        phone as phone,
        address as address,
        email as email,
        qq as qq,
        create_time as createTime,
        create_user as createUser,
        remark as remark,
        use_flag as useFlag,
        del_flag as delFlag

    </sql>

    <sql id="WHERE_SQL">
        <if test="id != null and id!=''">AND id = #{id}</if>
        <if test="name != null and name!=''">AND name = #{name}</if>
        <if test="age != null and age!=''">AND age = #{age}</if>
        <if test="phone != null and phone!=''">AND phone = #{phone}</if>
        <if test="address != null and address!=''">AND address = #{address}</if>
        <if test="email != null and email!=''">AND email = #{email}</if>
        <if test="qq != null and qq!=''">AND qq = #{qq}</if>
        <if test="createTime != null and createTime!=''">AND create_time = timestamp(now())</if>
        <if test="createUser != null and createUser!=''">AND create_user = #{createUser}</if>
        <if test="remark != null and remark!=''">AND remark = #{remark}</if>
        <if test="useFlag != null and useFlag!=''">AND use_flag = #{useFlag}</if>
        <if test="delFlag != null and delFlag!=''">AND del_flag = #{delFlag}</if>

    </sql>

    <sql id="UPDATE_SQL">
        <set>
            <if test="id!=null and id!=''">id=#{id},</if>
            <if test="name!=null and name!=''">name=#{name},</if>
            <if test="age!=null and age!=''">age=#{age},</if>
            <if test="phone!=null ang phone!=''">phone=#{phone},</if>
            <if test="address!=null and address!=''">address=#{address},</if>
            <if test="email!=null and email!=''">email=#{email},</if>
            <if test="qq!=null and qq!=''">qq=#{qq},</if>
            <if test="createTime!=null and createTime!=''">create_time=timestamp(now()),</if>
            <if test="createUser!=null and createUser!=''">create_user=#{createUser},</if>
            <if test="remark!=null and remark!=''">remark=#{remark},</if>
            <if test="useFlag!=null and useFlag!=''">use_flag=#{useFlag},</if>
            <if test="delFlag!=null and delFlag!=''">del_flag=#{delFlag}</if>

        </set>
    </sql>
</mapper>
